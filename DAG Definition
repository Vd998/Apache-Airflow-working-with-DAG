A typical DAG definition block looks like this.

# define the DAG
dag = DAG(
    dag_id='YOUR DAG ID',
    default_args=default_args,   ###AS MENTIONED IN DAG ARGUMENT###
    description='DAG DESCRIPTION',
    schedule_interval=timedelta(days=1),  ###IT WILL PERFORM THE CODED TASK EACH DAY###
)
